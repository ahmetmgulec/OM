{
  "name": "OM",
  "license": "ISC",
  "type": "module",
  "scripts": {
    "build": "spacetime build --module-path .",
    "generate-ts": "spacetime generate --lang typescript --out-dir ts-codegen --module-path .",
    "publish": "spacetime publish mytestapp --module-path . --delete-data -y"
  },
  "dependencies": {
    "spacetimedb": "^2.0.4"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}